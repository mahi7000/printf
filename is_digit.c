#include "main.h"

/**
 * is_digit - function that checks whether is number
 * @c: char to check
 *
 * Return: 1 or 0
 */

int is_digit(char c)
{
	if (c >= '0' && c <= '9')
		return (1);


	return (0);
}
